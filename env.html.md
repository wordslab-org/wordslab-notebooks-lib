# wordslab-notebooks-lib.env


<!-- WARNING: THIS FILE WAS AUTOGENERATED! DO NOT EDIT! -->

## wordslab-notebooks environment variables

WordslabNotebooksEnv class is the entry point to explore the
wordslab-notebooks environment variables.

------------------------------------------------------------------------

### WordslabEnv

``` python

def WordslabEnv(
    
):

```

*Initialize self. See help(type(self)) for accurate signature.*

``` python
env = WordslabEnv()
```

### wordslab-notebooks version and platform

``` python
env.version
```

    '2026-01'

``` python
env.platform
```

    'WindowsSubsystemForLinux'

### wordslab-notebooks external urls

``` python
env.url_dashboard
```

    'http://192.168.1.16:8888'

``` python
env.url_openwebui
```

    'https://192.168.1.16:8882'

``` python
env.url_jupyterlab
```

    'https://192.168.1.16:8880'

``` python
env.url_vscode
```

    'https://192.168.1.16:8881'

``` python
env.url_userapp1
```

    'https://192.168.1.16:8883'

``` python
env.url_userapp2
```

    'https://192.168.1.16:8884'

``` python
env.url_userapp3
```

    'https://192.168.1.16:8885'

``` python
env.url_userapp4
```

    'https://192.168.1.16:8886'

``` python
env.url_userapp5
```

    'https://192.168.1.16:8887'

### wordslab-notebooks internal ports

``` python
env.port_ollama
```

    11434

``` python
env.port_vllm
```

    8000

``` python
env.port_docling
```

    5001

``` python
env.port_userapp1
```

    '8883'

``` python
env.port_userapp2
```

    '8884'

``` python
env.port_userapp3
```

    '8885'

``` python
env.port_userapp4
```

    '8886'

``` python
env.port_userapp5
```

    '8887'

### wordslab-notebooks install directories

``` python
env.dir_home
```

    '/home'

``` python
env.dir_scripts
```

    '/home/wordslab-notebooks-2026-01'

``` python
env.dir_python
```

    '/home/python'

``` python
env.dir_workspace
```

    '/home/workspace'

``` python
env.dir_models
```

    '/home/models'

### wordslab-notebooks applications install directories

``` python
env.dir_openwebui
```

    '/home/open-webui'

``` python
env.dir_jupyterlab
```

    '/home/jupyterlab'

``` python
env.dir_vscode
```

    '/home/code-server'

``` python
env.dir_ollama
```

    '/home/ollama'

``` python
env.dir_docling
```

    '/home/docling'

### wordslab-notebooks applications data directories

``` python
env.dir_openwebui_data
```

    '/home/workspace/.openwebui'

``` python
env.dir_jupyterlab_data
```

    '/home/workspace/.jupyter'

``` python
env.dir_vscode_data
```

    '/home/workspace/.codeserver'

``` python
env.dir_docling_data
```

    '/home/workspace/.docling'

### wordslab-notebooks models directories

``` python
env.dir_models_ollama
```

    '/home/models/ollama'

``` python
env.dir_models_vllm
```

    '/home/models/huggingface'

``` python
env.dir_models_hugginface
```

    '/home/models/huggingface'

``` python
env.dir_models_fastai
```

    '/home/models/fastai'

``` python
env.dir_models_pytorch
```

    '/home/models/torch'

``` python
env.dir_models_keras
```

    '/home/models/keras'

``` python
env.dir_models_tensorflow
```

    '/home/models/tfhub_modules'

``` python
env.dir_models_docling
```

    '/home/models/docling'

### wordslab-notebooks default models

``` python
env.default_model_chat
```

    'gemma3:27b'

``` python
env.default_model_code
```

    'qwen3:30b'

``` python
env.default_model_agent
```

    'glm-4.7-flash:q4_K_M'

``` python
env.default_model_embedding
```

    'embeddinggemma:300m'

``` python
env.self.default_model_context_length
```

## User-defined environment variables

You can save user-defined environment variables in the following file
and they will be loaded by wordslab-notebooks each time a new shell is
created:

> $WORDSLAB_WORKSPACE/.secrets/user-env.bashrc

This module provides Python functions to programmatically manage these
user-defined environment variables.

They should primarily be used to register API keys use to connect to
external services.

------------------------------------------------------------------------

### WordslabEnv.write_user_env_var

``` python

def write_user_env_var(
    var_name:str, var_value:str
)->None:

```

*Write or update an environment variable in*
$WORDSLAB_WORKSPACE/.secrets/user-env.bashrc

The file is created if it doesn’t exist. The variable is created if it
doesn’t exist and updated if it already exist. The value is safely
quoted for Bash while preserving $VAR references.

The new variable value is automatically injected in the current python
process. The environment variable will be automatically loaded each time
you start a new shell. But you need to restart all the currently running
shells and other processes to see the new variable or value.

------------------------------------------------------------------------

### WordslabEnv.read_user_env_var

``` python

def read_user_env_var(
    var_name:str
)->Optional:

```

*Read a variable value from*
$WORDSLAB_WORKSPACE/.secrets/user-env.bashrc

Returns the raw (unexpanded) value, or None if not found.

``` python
env.write_user_env_var("SECRETS_PATH", "$WORDSLAB_WORKSPACE/.secrets")
```

``` python
# !cat $WORDSLAB_WORKSPACE/.secrets/user-env.bashrc
```

``` python
os.environ["SECRETS_PATH"]
```

    '/home/workspace/.secrets'

``` python
env.read_user_env_var("SECRETS_PATH")
```

    '$WORDSLAB_WORKSPACE/.secrets'

## Connections to external services

### OpenRouter

------------------------------------------------------------------------

### WordslabEnv.setup_openrouter

``` python

def setup_openrouter(
    openrouter_api_key:str
)->None:

```

``` python
# env.setup_openrouter(openrouter_api_key="sk-...")
```

``` python
# env = WordslabEnv()
# env.cloud_openrouter_api_key
```

### Replicate

------------------------------------------------------------------------

### WordslabEnv.setup_replicate

``` python

def setup_replicate(
    replicate_api_token:str
)->None:

```

``` python
# env.setup_replicate(replicate_api_token="r8_...")
```

``` python
# env = WordslabEnv()
# env.cloud_replicate_api_token
```

### Tavily

------------------------------------------------------------------------

### WordslabEnv.setup_tavily

``` python

def setup_tavily(
    tavily_api_key:str
)->None:

```

``` python
# env.setup_tavily(tavily_api_key="tvly-...")
```

``` python
# env = WordslabEnv()
# env.cloud_tavily_api_key
```

### Ollama cloud

------------------------------------------------------------------------

### WordslabEnv.setup_ollamacloud

``` python

def setup_ollamacloud(
    ollama_api_key:str
)->None:

```

``` python
# env.setup_ollamacloud(ollama_api_key="0d2...")
```

``` python
# env = WordslabEnv()
# env.cloud_ollama_api_key
```

### Github

------------------------------------------------------------------------

### WordslabEnv.setup_github

``` python

def setup_github(
    git_user_name:str, git_user_email:str, github_username:str, github_access_token:str
)->None:

```

``` python
# env.setup_github(git_user_name="John Doe", git_user_email="john.doe@email.com", github_username="johndoe", github_access_token="ghp_...")
```

    Wrote Github credentials

### Huggingface

------------------------------------------------------------------------

### WordslabEnv.setup_huggingface

``` python

def setup_huggingface(
    huggingface_access_token:str
)->None:

```

``` python
# env.setup_huggingface(huggingface_access_token="hf_...")
```

``` python
# env = WordslabEnv()
# env.cloud_huggingface_access_token
```

### Pypi

------------------------------------------------------------------------

### WordslabEnv.setup_pypi

``` python

def setup_pypi(
    pypi_api_token:str
)->None:

```

``` python
# env.setup_pypi(pypi_api_token="pypi-...")
```

    Wrote Pypi credentials
